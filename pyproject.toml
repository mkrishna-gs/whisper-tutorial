[project]
name = "whisper-tutorial"
version = "0.1.0"
description = "Extract audio from video and transcribe it with OpenAI Whisper"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "openai-whisper>=20250625",
]
